on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: farhadctg222/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.lawyer-bangladesh.com
        username: lawyer-bangladesh@lawyer-bangladesh.com
        password: ${{ secrets.FTP_PASS }}
        port:${{ftp_port}
